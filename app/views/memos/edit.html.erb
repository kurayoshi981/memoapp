 <h3>へんしゅう</h3>
  <%= form_tag("/memos/"+@memo.id.to_s,:method=>:patch) do %>
    <div class="form-group">
      <%= label_tag "form-title" , "たいとる" %>
      <%= text_field :memos, :title, id: "form-title" , class: "form-control" , value: @memo.title %>
    </div>

    <div class="form-group">
      <%= label_tag "form-content" , "ないよう" %>
      <%= text_field :memos, :body, id:"form_content", class: "form-control",value: @memo.body %>
    </div>
     <%= select_tag 'memos[category_id]', options_from_collection_for_select(@categories, :id, :name) %>


      <%= submit_tag "Update" , class: "btn btn-primary" %>
  <% end %>
</div>
